To install the htmltemplate module from source, cd to the htmltemplate-2.0.0 directory and run:

	python3 setup.py install

Also see the 'doc' directory for full manual and tutorials, and the 'sample' directory for examples of use.

